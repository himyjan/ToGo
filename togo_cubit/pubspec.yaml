name: togo_cubit
description: Store and Cart example using Cubit in Flutter.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  gap: ^2.0.1
  uuid: ^3.0.7
  equatable: ^2.0.5
  google_fonts: ^3.0.1
  cached_network_image: ^3.2.3
  scrollable_positioned_list: ^0.3.5
  flutter_elegant_number_button: ^0.0.6
  badges: ^2.0.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  supabase: ^1.2.0
  bloc: ^8.1.0
  flutter_bloc: ^8.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
